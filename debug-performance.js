// Quick debug script to test if store changes are causing issues
console.log('Testing store performance...');

// Test if the store initialization has issues
try {
  console.log('Store test completed successfully');
} catch (error) {
  console.error('Store initialization error:', error);
}