import { ColorPalette } from './types';
import { lightPaperColors, darkPaperColors } from './paperColors';

// Export paper-themed colors as our main color system
export const lightColors: ColorPalette = lightPaperColors;

export const darkColors: ColorPalette = darkPaperColors;

// Note: Now using paper-themed color system from paperColors.ts
// This combines the warm, organic feel of paper notebooks with Apple's 
// accessibility standards and semantic color approach